# עומק הים המואר — מפת התיקיה

הספציפיקציה החזותית והאינטראקטיבית של אבני יסוד.
מקור: ChatGPT, מאי 2026. נוצר אחרי השוואה ל-Lovable שנדחתה (אילוסטרציות גנריות).

## מבנה התיקיה

| קובץ | תוכן |
| --- | --- |
| `book-of-environments.md` | 22 הסביבות התת-ימיות, פר-שלב פדגוגי. מקור-האמת הוויזואלי-פדגוגי. |
| `design-system.md` | DS מלא — עקרונות, פלטה, טיפוגרפיה, grid, רכיבי UI, בועות, glow, אנימציה, ספריית SVG. |
| `game-envelopes.md` | 6 מעטפות המשחק (tap-match / drag-build / trace-write / shared-read / mini-task / memory-sort). |
| `noni-home.md` | פינת התמנון הביתית — בית נוני + חנות פנינים. |
| `interactive-primitives.md` | 10 פרימיטיבים אינטראקטיביים חוזרים (BubbleStream, SoftRipple, PedagogicGlow, RTLFlowLine, SoftSnap, OctoGesture, SuccessTrail, HintBubble, SceneTransitionBubbles, MasterySouvenirAppear). |
| `product-screens.md` | 9 מסכי המוצר (פתיחה, מפה, פעילות, בית נוני, חנות, סיום פעילות, התקדמות תלמיד/ה, דשבורד מורה, פרופיל למידה). |
| `claude-code-brief.md` | 2 מסמכי הנחיות לבנייה: כללי + MVP Build Brief עם prompt מוכן. |

## תיקוני המלצה שעדיין לא בוצעו ב-ChatGPT

המלצות שאוסיף לתיקון לפני בנייה. לא נכנסו לקבצים — הם נשמרו AS-IS מ-ChatGPT.

1. **שלב 7 — "מערת זרמי המילים"** → להחליף ל**"לגונת הזרמים"** או **"זרמי המילים השלושה"**. המילה "מערה" מנוגדת לאסטרטגיה (אסוציאציית חושך/פחד), גם כשמוסבר "פתוחה ומוארת".
2. **שלב 18 — "מערת הרגשות הרכה"** → להחליף ל**"בריכת הרגשות"** או **"מפרץ ההרגשה"**. אותה סיבה.
3. **שלב 18 — "פרצופים"** → להחליף ל**"פנים"** או **"הבעות פנים"**. "פרצוף" טעון שלילית בעברית ילדית.
4. **שלב 12 — "אוצר המילים בעל-פה"** → להחליף ל**"תיבת האוצר של נוני"**. שאר הסביבות בעלות שמות פיגורטיביים, רק כאן יש שם פדגוגי-טכני.

## הקשר לארכיטקטורה הקיימת של אבני יסוד

- `architecture-mvp.md` (מסמך-אם של אבני יסוד) קובע: 22 איים = שלבים פדגוגיים, מנוע BKT, דשבורד בוקר למורה. הספר הזה הוא **העטיפה החזותית** של אותה ארכיטקטורה — הוא לא מחליף, הוא לובש.
- `engine/` (המנוע הקיים) — מספק את ה-BKT, ה-vocab-bank, ה-TTS עברית, את הדשבורד למורה. הספר הזה מספק את ה-presentation layer.
- כל סביבה כאן מתאימה ל-island_id מהמערכת הקיימת.
- מטבע "פנינים" כאן = המקבילה של "אבני-קסם" ב-`gamification-spec.md`. שני השמות תקפים — יוכרע בפיילוט.

## הצעד הבא

לפי `claude-code-brief.md` סעיף "First Build Recommendation" + ה-MVP Build Brief — לבנות Vertical Slice של שלב 1 בלבד ("מפרץ הבועות המזמרות") כולל: מסך פתיחה, מפה, פעילות tap-match + drag-build בסיסי, פידבק מדורג, פנינים, מזכרת שליטה "טבעת הד", בית נוני, דשבורד מורה mock.

לא לבנות את כל 22 לפני שה-Vertical Slice עובד על טאבלט עם ילד.ה אמיתי.ת.
