# יומן גלים · גרנטה

יומן התזמורת. נכתב על ידי התזמורת בלבד — סוכנים לא נוגעים בו.
המקור לתכנון: `granta/orchestra.html` · האפיון: `granta/SPEC.md`

---

## גל 1 · 29.8.2026 · שלב 0

**רוחב 5.** כל חמש המשימות ללא תלויות, שטחי בעלות זרים לחלוטין — אומת מראש בסימולציה (אפס התנגשות).

| # | משימה | נתיב | בעלות |
|---|---|---|---|
| G-01 | שלד granta/ + חוזה לקוח משותף | ארכיטקטורה | `granta/assets/granta.js` |
| G-02 | סטנדרט UX + נגישות (מקור אמת) | חוויה | `granta/_handoff/GRANTA-UX-STANDARD.md` |
| G-03 | מיגרציה 0100 · ליבה + RLS | נתונים | `supabase/migrations/0100_granta_core.sql` |
| G-04 | קטלוג כללי ELIG + נוסחי הסבר | תוכן | `granta/_data/rules-elig.json` |
| G-30 | פרטיות · תנאים · ייפוי-כוח | תפעול | `granta/legal/` |

**הנחיות שנכללו בכל bootstrap:** בעלות בלעדית · רשימת אסור-לגעת מחושבת · בלי git commit/push · בלי עריכת SPEC.md · בלי מחיקות · פורמט דיווח JSON חובה.

**סטטוס:** שוגר · ממתין לדיווחים.

---

**סטטוס: הושלם. 5/5 אומתו עצמאית.**

| # | אימות שהתזמורת ביצעה בעצמה | תוצאה |
|---|---|---|
| G-01 | תחביר ES module · ספירת ייצואים · הצלבת 18 שמות טבלאות מול §4 · grep על מפתחות קשיחים | ✅ |
| G-02 | מדידת ניגודיות חוזרת של כל הפלטה מול לבן · ספירת מזהי כללים | ✅ ממצא אושר |
| G-03 | (הסוכן אימת ב-libpg_query) + הצלבת סכימה↔קוד על ידי התזמורת | ✅ + 4 באגים |
| G-04 | JSON תקין · 15 כללים · סכום משקלים=100 · אפס doc_url מומצא · כל detect.field מוצהר | ✅ |
| G-30 | אזהרת טיוטה ×3 · אפס חוקים מומצאים · אפס מיילים · אפס אזכור אורט · מחירים | ✅ |

### ממצאי אינטגרציה — נתפסו רק בהצלבת צרכן↔ספק, לא בבדיקה מבודדת

| באג | תוקן ב |
|---|---|
| `granta.js` כתב `actor_role`; הסכימה מגדירה `actor_kind` | `granta.js` |
| הערך שנכתב היה תפקיד (`org_admin`) ולא סוג שחקן — נכשל ב-CHECK | `granta.js` |
| `diff: null` מפורש מול עמודה `NOT NULL DEFAULT '{}'` | `granta.js` |
| `.select()` אחרי insert לליד — לאנונימי אין SELECT, הגשה תקינה הייתה נכשלת | `granta.js` |

### שינויי תוכנית שנגזרו מהגל
- **G-31** קובץ קונפיג — נחשף על ידי G-01. בלעדיו כל מסך נופל.
- **G-32** מסך התחברות — נחשף על ידי G-01. guard הפנה ל-404.
- **G-33** הצלבת ייפוי-כוח↔שער אישורים — נחשף על ידי G-30. המסמך המשפטי מחייב את הקוד.
- **SPEC §10** — נוסף סיכון החרגת מוסדות חינוך על הופה. נחשף על ידי G-04.

---

## גל 2 · שלב 0

| # | משימה | בעלות |
|---|---|---|
| G-05 | מערכת עיצוב: tokens + רכיבים + גלריה | `granta/assets/tokens.css` · `granta.css` · `_gallery.html` |
| G-06 | Edge Function · סורק אתר | `supabase/functions/granta-scan-website/` |
| G-31 | קונפיג + היגיינת סודות | `granta-config.example.js` · `.gitignore` · `_ops/secrets-hygiene.md` |

**ידע שהתזמורת העבירה בין הגלים** (סוכן לא יכול היה לדעת לבד):
- G-05 קיבל את ממצא הניגודיות המדוד + את ה-class hooks המדויקים של `granta.js`.
- G-06 קיבל את חוזה ה-`_fields` מ-G-04, את הכלל 🔴 `null`≠`false`, ואת ערכי הבסיס של hopa.org.il לאימות.
- G-31 קיבל את מפתחות הקונפיג שהם בדיוק מה ש-`granta.js` מחפש.

**סטטוס:** שוגר.

**סטטוס: הושלם. 3/3 אומתו עצמאית.**

| # | אימות התזמורת | תוצאה |
|---|---|---|
| G-05 | grep צבע/תכונות-פיזיות=0 · מדידה ב-390 אמיתי דרך iframe: scrollWidth 375≤390 · 7 class hooks קיימים · detail מוסתר | ✅ |
| G-06 | **הרצה חיה מול hopa.org.il מול ערכי אמת ידניים: 8/8** · דומיין מת → 17 null ורק site_reachable=false · SSRF: localhost ומטא-דאטה נחסמו, 18 null אפס false | ✅ |
| G-31 | .gitignore 6 הוספות 0 מחיקות · config מוחרג/example לא · סריקת סודות על כל הריפו: אין דלף | ✅ |

**פרצה שנסגרה:** `looksLikeServiceRole` פענח JWT בלבד — מפתח בפורמט החדש `sb_secret_…` היה עובר **בשקט**. הורחב לזיהוי לפי קידומת. מבחן 6 פורמטים: 6/6.

**טעות של התזמורת שתוקנה:** מדדתי 3 יעדי מגע מתחת ל-44px וחשדתי ב-G-05. הם תיבות רדיו של 20px בתוך תוויות של 71–93px — התווית היא יעד המגע. הסוכן צדק.

---

## 🔴 קטיעת סשן

הסשן נקטע באמצע גל 3. **אפס מתוך חמשת התוצרים נחתו**, והסוכנים לא היו ניתנים לשחזור. גלים 1–2 נשארו שלמים על הדיסק. הגל שוגר מחדש, מחוזק בכל מה שנלמד — כולל מלכודת ה-`innerWidth`.

**לקח:** שום דבר לא היה ב-git. הענף הפעיל אינו קשור לגרנטה. לפני הגל הבא — ענף `feat/granta-phase-0` וקומיט.

---

## גל 3 · שלב 0 (שיגור חוזר)

| # | משימה | בעלות |
|---|---|---|
| G-07 | מנוע זכאות · ציון ופסיקה | `supabase/functions/granta-eligibility/` |
| G-08 | A0 · דף הנחיתה | `granta/index.html` |
| G-09 | A1 · טופס בדיקת זכאות | `granta/check.html` |
| G-11 | C2 · קונסולת לידים | `granta/ops/leads.html` |
| G-32 | מסך התחברות | `granta/app/login.html` |

### 🔴 סבב שני של באגי אינטגרציה — ההצלבה הראשונה שלי פספסה אותם

G-09 מצא ש-`leads.create()` לא תואם לסכימה, וכל הגשה אנונימית הייתה נופלת:

| באג | למה ההצלבה הראשונה פספסה |
|---|---|
| `utm` נשלח כאובייקט; הסכימה שטוחה (`utm_source`…) | סיננתי שמות לפי קו-תחתון — `utm` נשמט |
| `status` קיים כעמודה אבל **אינו ב-GRANT ל-anon** | בדקתי קיום עמודה, לא הרשאת כתיבה |
| `gclid` נאסף — אין לו עמודה | אותו סינון |

**התיקון:** UTM שטוח · `status` לא נשלח (ברירת המחדל מספקת את ה-WITH CHECK) · `gclid` הוסר.

**הכלי שנבנה כדי שזה לא יחזור:** `granta/_orchestra/check-schema-contract.mjs` — מצליב payloads מול עמודות **ומול ה-GRANT ברמת-העמודה**. אומת בשני הכיוונים: מזהה את שני הבאגים המקוריים (exit 1) ונקי על הקוד המתוקן (exit 0).

### אימות מקצה לקצה שהתזמורת הריצה בעצמה
סריקת הופה שלכדתי → מנוע ההכרעה:
- **בלי השלמה ידנית:** ציון 100, אפס הפרות, פסיקה **"דורש בדיקה"** — 4 כללים ממתינים לשדות שסריקה לא קובעת.
- **אחרי 3 השדות הידניים:** **"כנראה זכאית"**.
- הסייג "האישור בידי Google בלבד" מופיע בשתי התשובות.

זו ההתנהגות הנכונה: המערכת לא מכריזה על זכאות שהיא לא יכולה לאמת.

**14/14 בדיקות המנוע עוברות**, כולל שלושת מקרי המבחן מהקטלוג ושומר-הסדק.
