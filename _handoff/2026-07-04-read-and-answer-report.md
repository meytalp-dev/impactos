# דוח M2 · וריאנט הבנת-הנקרא (stage-14-read-and-answer) + תיקון מיפוי מאי

**תאריך:** 4.7.2026 · **משימה:** M2 בלוח השלמת התרגולים

## מה נבנה

### 1. `avnei-yesod/underwater-app/stage-14-read-and-answer.html` — חדש
וריאנט קריאה של "להאזין ולענות": הקטע מוצג **כטקסט מנוקד מלא על המסך** (Heebo גדול,
כל משפט בשורה משלו via `white-space:pre-line`) — **בלי הקראה אוטומטית, זו כל הפואנטה**.
- משתמש במכניקה הקיימת `mechanic-listen-mcq.js` במצב `mode:'read'` (ADAPT-8) — אותו מבנה נתונים.
- כפתור הקראת-הקטע מוסתר (`display:none`); כפתור רמקול קטן ליד **השאלה בלבד** נשאר (הנגשה).
- חוקי עולם: `scene-bg` PNG ✓ · `tokens.css` (פלטת פסטל-שונית) ✓ · נוני PNG + בועת דיבור ✓
  (happy על נכון, thinking על שגוי) · טקסט ילד מנוקד בלשון רבים ✓ · touch-min 56px ✓.
- מסך פתיחה = בחירת קטע (3 כרטיסים); deep-link `?text=shuk|whales|bees` מדלג ישר לקטע.

### 2. `data/island-14-reading/passages-read.json` — חדש · 🔴 טעון אישור פדגוגי של מיטל
קטעי מאי לא היו קיימים כקטעים מלאים בשום מקום (בבנק `questions-grade1.json` יש רק
**משפטים בודדים** על דבורה/דבורים/לווייתן — stems של שאלות may-*). נכתבו 3 קטעים
חדשים (3–4 משפטים, ניקוד מלא, כל האותיות/תנועות נלמדו עד מאי) + 3 שאלות לכל קטע:

| topic | קטע | סוג | הערת אוצר-מילים |
|---|---|---|---|
| `shuk` | יוֹם שׁוּק בַּגִּנָּה | סיפור | גיבורה "דְּבוֹרָה" — עקבי עם שאלות may-w1 בבנק |
| `whales` | לִוְיְתָנִים | טקסט מידע | לִנְשֹׁם |
| `bees` | דְּבוֹרִים | טקסט מידע | חֶרֶק · צוּף · חֲרוּצָה |

🔴 **המילים המסומנות + כל הניקוד והשאלות — לאישור מיטל לפני פיילוט.**
ה-flag גם ב-`_meta.pedagogical_review` בקובץ עצמו.

### 3. `mechanic-listen-mcq.js` — שינוי מינימלי
`playText(audioKey, text, force)` — פרמטר `force` חדש: במצב read, לחיצה מפורשת על
רמקול-השאלה כן מקריאה (הנגשה); ההקראה האוטומטית והקראת-הקטע נשארות חסומות.
אי ההאזנה הקיים — ללא שינוי התנהגות (נבדק, ראו למטה).

### 4. `demo/teacher-curriculum.html` — resolvePractice + קטגוריה חדשה
- כלל חדש **לפני** כללי ההאזנה: `/^קריאת (סיפור|טקסט|תיאור|פסקה)|^טקסט מידע|איתור מידע בטקסט|מבנה טקסט מידע/`
  → `stage-14-read-and-answer.html`, tag "הבנת הנקרא", קטגוריה חדשה `c-read` (טורקיז-כחלחל).
- קטעי מאי מקבלים deep-link לקטע הספציפי (שוק/לווייתנים/דבורים). לב: זיהוי לווייתן
  ברגקס גמיש `/לוו?יי?ת[נן]/` (כתיב מלא/חסר, יחיד/רבים).
- ימי האזנה אמיתיים ("הבנת הנשמע", "האזנה לסיפור", "רצף אירועים") נשארו על stage-14 הקיים.

**ימים שהשתנו ממיפוי שגוי/חסר → הבנת הנקרא:** מרץ: "קריאת תיאור + התאמה לדמות",
"קריאת תיאור ארוך" (היו gap), "טקסט מידע + ארבע שאלות" (היה האזנה) · מאי: "קריאת סיפור
יום שוק", "קריאת טקסט מידע (לווייתנים)", "טקסט מידע על דבורים", "איתור מידע בטקסט",
"מבנה טקסט מידע" (היו האזנה) · יוני: "קריאת פסקה עצמית" (היה האזנה).
**מחוץ לסקופ (נשארו כפי שהיו):** "טקסט הוראות", "טקסט סיפורי + ציטוט", "השוואה בין שני טקסטים".

## logging
לפי הדפוס הקיים של אי 14: `AvneiOralSkillAdapter.recordAttempt` → BKT (אי 14, skills
identify-hero/sequence/inference) + `AvneiEventLogger.logActivityResult`
(variant `read_comprehension_pick`). אין target_letter → EPA מדולג (G4, מכוון).

## בדיקות (עברו ✓)
- `scripts/e2e/smoke-read-and-answer.js` — picker 3 קטעים · deep-link whales · כפתור
  הקראת-קטע מוסתר · רמקול-שאלה גלוי · זרימת 3 שאלות → סיכום → חזרה · 0 שגיאות קונסולה.
- `scripts/e2e/smoke-curriculum-read-mapping.js` — 15 assertions על המיפוי (מאי/מרץ/יוני/אפריל/ספטמבר), כולל שימי האזנה לא נפגעו.
- רגרסיה: stage-14-listen-and-answer נטען, כפתור הקראת-קטע גלוי, מתקדם בין פריטים, 0 שגיאות.
- צילומי מסך: `scripts/e2e/screenshots/smoke-read-and-answer-{passage,summary}.png`.

## פתוח / להמשך
1. 🔴 אישור פדגוגי לקטעים ולשאלות (מיטל).
2. MP3 (קול נוני ElevenLabs) לשאלות — כרגע רמקול-השאלה נופל ל-Web Speech (הנגשה בלבד).
3. קטעים נוספים: מרץ ("טקסט מידע + ארבע שאלות", תיאורי דמויות) ויוני ("קריאת פסקה עצמית")
   מקבלים כרגע את ה-picker הכללי עם 3 קטעי מאי — כדאי תוכן ייעודי בהמשך.
4. "טקסט הוראות" + "השוואה בין שני טקסטים" — מכניקות אחרות, לא בסקופ M2.
